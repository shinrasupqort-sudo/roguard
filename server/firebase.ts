// Firebase integration has been removed. Stub exports provided so that
// any legacy imports continue to compile but nothing actually happens.

export function logAuthEvent(..._args: any[]) {
  // no-op
}

export const realtimeDb = null as any;
